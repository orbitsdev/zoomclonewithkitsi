import 'package:flutter/material.dart';

class JointMeetingScreen extends StatelessWidget {
const JointMeetingScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return   Container(child: Center(child: Text("Join Meeting")),);
  }
}